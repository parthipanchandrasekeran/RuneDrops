using UnityEngine;

/// <summary>
/// Disabled — orientation hacks were causing visual issues.
/// The SM-A032F renders portrait inverted. User should hold phone upside-down
/// until proper art/build pipeline handles this via Android manifest.
/// </summary>
public class ScreenFlipper : MonoBehaviour
{
    // Intentionally empty — all flip logic removed
}
