namespace RuneDrop.Runes
{
    public enum ComboType
    {
        None = 0,
        FlameTrail = 1,       // Fire + Wind
        BlinkDash = 2,        // Shadow + Wind
        ExplosiveShield = 3   // Earth + Fire
    }
}
