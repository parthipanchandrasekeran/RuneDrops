namespace RuneDrop.Runes
{
    public enum RuneType
    {
        None = 0,
        Fire = 1,      // Damage nearby obstacles
        Wind = 2,      // Slow fall
        Shadow = 3,    // Phase through obstacles
        Earth = 4      // Shield (absorb one hit)
    }
}
